# 刻意不含 postgres.dsn，用於 BDD scenario：「設定檔缺少必要欄位時拒絕啟動」。
server:
  port: 8080
  debug: true
  shutdown_timeout: 5s

postgres:
  max_open_conns: 10

redis:
  addr: "localhost:6379"
  password: ""
  db: 0

log:
  level: "info"
